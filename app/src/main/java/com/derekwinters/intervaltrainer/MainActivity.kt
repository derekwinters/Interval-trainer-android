package com.derekwinters.intervaltrainer

import android.app.Activity

/**
 * The launcher activity (BUILD-012).
 *
 * Empty on purpose: it exists so the module assembles into an APK with something to launch. It
 * gets a user interface when there is a specified feature to put in it.
 */
class MainActivity : Activity()
