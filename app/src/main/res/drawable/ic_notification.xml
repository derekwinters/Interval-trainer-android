<?xml version="1.0" encoding="utf-8"?>
<!-- The notification's status-bar icon (SVC-020). A notification's small icon must be a flat,
     single-alpha silhouette — the system supplies its own colour — so this is a plain diamond
     rather than an attempt at a literal clock face. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M12,2 L22,12 L12,22 L2,12 Z" />
</vector>
